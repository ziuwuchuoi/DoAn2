import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const NotKnow = () => {
  return (
    <View>
      <Text>NotKnow</Text>
    </View>
  );
};

export default NotKnow;

const styles = StyleSheet.create({});
